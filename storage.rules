rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /moments/{coupleId}/{fileName} {
      allow read, write: if request.auth != null &&
        request.auth.uid in firestore.get(/databases/(default)/documents/couples/$(coupleId)).data.members;
    }
  }
}
